services:
  bot:
    build: .
    container_name: elecho-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./firebase-credentials.json:/app/firebase-credentials.json:ro
    environment:
      - FIREBASE_CREDENTIALS=/app/firebase-credentials.json
